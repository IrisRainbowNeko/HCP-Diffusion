_base_: [cfgs/infer/text2img.yaml]

part_path: ''

merge:
  group1:
    type: 'unet'
    base_model_alpha: 0.0 # 基础模型权重0.0，part部分新模型权重1.0，表示用part模型覆盖原有模型
    part:
      - path: ${part_path}
        alpha: 1.0